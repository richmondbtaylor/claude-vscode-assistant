# SDD ledger — plan: docs/superpowers/plans/2026-08-26-bob-startup-leads.md

Spec: docs/superpowers/specs/2026-08-26-bob-startup-leads-design.md (read, reachable)
Worktree: C:/Users/richm/.claude/.worktrees/bob-startup-leads on feat/bob-startup-leads
Branch base: f8bc11b

## Pre-flight scan

### Cross-task rows (tasks sharing a file or interface)

| Producer | Consumer | Produces vs consumes | Finding |
|---|---|---|---|
| T1 | T2 | `company_id`, `write_jsonl` | OK. Interfaces block also lists `norm_phone`, which T2's code never calls. Over-declaration only. |
| T1 | T3 | `company_id`, `write_jsonl` | OK. Interfaces block lists `registrable_domain`, not imported by the code. Over-declaration only. |
| T1 | T4 | `norm_phone`, `registrable_domain`, `company_id` | OK, all three used in `place_to_record`. |
| T1 | T6,T7,T9,T10,T11,T12,T13 | `norm_name`, `is_valid_person_name`, `read_jsonl`, `write_jsonl` | **CONFLICT C1** (dependency declaration, see below). |
| T3 | T5, T8 | `brave_search` | OK, signature matches both call sites. |
| T2,T3,T4 | T6 | `seed_sba.jsonl`, `seed_jobs.jsonl`, `seed_maps.jsonl` | OK, all three filenames agree with `dedupe.SEED_FILES`. |
| T6 | T5 | `companies.jsonl` | OK. Build order (T5 before T6) is intentionally not run order; the plan's run-order section has `dedupe` first and `resolve_domain --infile companies.jsonl` defaults to it. |
| T5 | T7 | `resolved.jsonl` | OK. |
| T7 | T8 | `sites.jsonl` | **CONFLICT C5**: nesting of `tech` / `has_pricing_page` / `has_careers_page` unspecified. |
| T8 | T9 | `signals.jsonl` | **CONFLICT C6**: T9 scores `sig["headcount"]`, T8's `main()` never sets it. |
| T9 | T10 | `scored.jsonl`, `tier` field | OK. |
| T10 | T11 | `enriched.jsonl`, `contact_*` fields | OK. |
| T11 | T12 | `hooks.jsonl`, `hook` field | OK. |
| T12 | T13 | `qa_report.json` | **CONFLICT C7** (gate deadlock) and **C8** (row source unstated). |

### Per-task self-agreement rows

| Task | Tests vs code | Files created vs later touched | Finding |
|---|---|---|---|
| T1 | 3 assertions contradict the implementation | `lib/`, `config.py` created; touched by every later task | **C2**, **C3**, **C4** |
| T2 | agrees | `seed_sba.py` only | `stream_csv` breaks on newlines inside quoted CSV fields; plan's Step 5 already flags this for QA. Accepted. |
| T3 | agrees | `seed_jobs.py`, later imported by T5 and T8 | OK. |
| T4 | agrees (verified `test_address_without_parseable_state_still_records` falls back to `"Miami FL"` split) | `seed_maps.py` only | OK. |
| T5 | agrees | `resolve_domain.py` only | OK. |
| T6 | agrees (verified merge precedence and 3-key collapse by hand) | `dedupe.py` only | OK. |
| T7 | agrees | `site_scrape.py` only | OK. |
| T8 | agrees | `signals.py` only | `linkedin_headcount` is defined but never called. See C6. |
| T9 | agrees (hand-computed: RICH scores 85 ≥ 75, THIN scores 4 < 35, `only 1 evidence families` contains `families`) | `score.py` only | OK. |
| T10 | agrees | `enrich_tier1.py` only | **C9**: docstring promises 7 waterfall steps, code implements 5-7 only. |
| T11 | agrees (hand-checked both generated hooks against `lint_hook`) | `hooks.py` only | OK. |
| T12 | agrees | `qa.py` only | OK in isolation; see C7 for the cross-task effect. |
| T13 | agrees | `upload_sheet.py` only | OK. |

### Rulings

**Ruling C1: convert to a uv project (`pyproject.toml`); drop PEP 723 headers from all modules.** — Every module below `lib/` imports `lib.normalize`, which needs `tldextract`, but `dedupe.py`, `score.py`, `hooks.py`, `qa.py` declare no dependencies and `signals.py` / `enrich_tier1.py` / `upload_sheet.py` omit `tldextract`. Under PEP 723 each script gets an isolated env containing only its own declared deps, so seven of thirteen modules fail at import. Separately, `uv run pytest` has no way to resolve pytest at all, so every test command in the plan is unrunnable as written. The user's own CLAUDE.md prescribes `uv init` / `uv add` / `uv run` for projects and reserves PEP 723 for throwaway scripts; this is a project. Task 1 gains a `pyproject.toml` declaring `httpx`, `beautifulsoup4`, `tldextract`, `rapidfuzz`, `playwright`, `google-api-python-client`, `google-auth`, `google-auth-oauthlib` and a `pytest` dev group. — **Cost if wrong:** headers get re-added later; no logic changes.

**Ruling C2: `norm_name` strips apostrophes before punctuation substitution.** — As written, `_PUNCT` turns `"Bob's"` into `"bob s"`, so the plan's own test expecting `"bobs plumbing and heating"` fails. Delete apostrophes rather than space-substituting them. — **Cost if wrong:** names with apostrophes match slightly differently during dedupe.

**Ruling C3: `_NAME_TOKEN` must permit an internal capital after a hyphen or apostrophe.** — `[A-Z][a-z'’-]{1,}` rejects `"O'Brien"` and `"Jean-Luc"`, both of which the plan's own tests assert are valid. Widen to allow a capital following `-` or `'`. — **Cost if wrong:** a slightly looser name filter; the stopword list still catches the junk cases.

**Ruling C4: Task 1's stated test count is 34, not 27.** — Miscount in the plan (5 + 7 + 6 + 4 + 12). The implementer should treat the enumerated cases as authoritative, not the total. — **Cost if wrong:** none, cosmetic.

**Ruling C5: `tech`, `has_pricing_page` and `has_careers_page` nest under `row["signals"]`; `email`, `email_status` and `phone` stay top-level.** — Task 9's `family_scores` reads the first three off `sig` and the last two off `row`, so this is the only nesting consistent with the scorer. — **Cost if wrong:** the money family silently reads zero and scores collapse; caught by Task 9's live distribution check.

**Ruling C6: Task 8's `main()` must call `linkedin_headcount` and set `sig["headcount"]`.** — Task 9 weights headcount at 40% of the scale family. Leaving it unset makes that term permanently zero while the code that computes it sits unused. `main()` opens one Playwright context with the saved LinkedIn session, iterates rows that have a domain, and writes the result. Rows where the lookup fails get `headcount: None`, which the scorer already tolerates. — **Cost if wrong:** a slow stage; if LinkedIn blocks, headcount stays None and scoring degrades exactly as it does today.

**Ruling C7: Task 12 demotes under-enriched Tier 1 rows to master instead of failing them.** — As written, `check_row` fails any tier1 row lacking `contact_name` or `hook`, and Task 13 refuses to upload while `failed > 0`. Since the waterfall cannot guarantee a contact for every row and `hook_for` returns `""` for rows with no matching signal, the two rules together deadlock the upload on the first unenriched row. `run_gates` will demote those rows to `master`, record `demoted_from_tier1` with a reason, and leave the genuinely blocking gates (contactability, liveness, invalid name, verified-without-address) as hard failures. — **Cost if wrong:** Tier 1 comes back smaller than 20%; the count is reported, so it is visible and reversible.

**Ruling C8: `upload_sheet.build_tabs` reads its rows from `data/hooks.jsonl`.** — That is the last file in the chain carrying every field the tabs render. — **Cost if wrong:** a one-line path change.

**Ruling C9: `waterfall` seeds `contact_*` from data already on the row before calling Hunter.** — The plan lists seven steps but the code starts at step 5, so contact data the site scrape already found is ignored and Hunter is billed for rows that were already satisfied. Steps 1 and 2 become a local seed from the row's existing fields; step 3 stays Florida-only per the plan; step 4 stays out of scope for this task since Task 8 already opens LinkedIn. — **Cost if wrong:** a small number of extra Hunter calls.

**Ruling C10: over-declared Interfaces blocks in Tasks 2 and 3 are not defects.** — The code is authoritative; the implementer imports what it uses. — **Cost if wrong:** none.

---

## Task log

Task 1: complete (commits f8bc11b..b8c827b, review clean) — spec ✅, quality approved, 37/37 tests
Task 1: minor (deferred): no `__init__.py` under `lib/` or `tests/`; relies on PEP 420 namespace packages plus pytest `pythonpath`. Fine today, brittle if a later task nests subpackages under `lib/`.
Task 1: ⚠️ resolved by controller — reviewer could not execute `uv run pytest`; implementer's RED/GREEN evidence in task-1-report.md covers it. Not a gap.
Task 2: fix round 1/5 (4 addressed, 0 open — _num non-finite guard; quote-safe CSV line streaming; decimal-jobs regression tests; report path; commits a0a87dc..15cff1a)
Task 2: complete (commits b8c827b..15cff1a, review clean) — spec ✅, quality approved, 48/48 tests
Task 2: note — live check found SBA writes JobsSupported as "9.0"; int("9.0") raised and silently rejected 200/200 rows on the first run. Any later task casting SBA-family numerics must parse through float() first.
Task 2: note — isfinite guard also closed a latent floor bypass: GrossApproval="inf" previously passed `inf < MIN_GROSS_APPROVAL` as False and leaked into output.

**Ruling C11: drop the trailing `.replace(" ", "")` from the ATS slug-to-name transform.** — The plan's transform glues multi-word slugs into one token (`rivera-mechanical` becomes `RiveraMechanical`). Both brief fixtures use separator-free slugs, so the common real-world shape was untested. The glued form breaks two things: `company_id` keys cross-lane identity on `norm_name`, which splits on whitespace, so the same company arrives from the SBA lane as "rivera mechanical" and from this lane as "riveramechanical" and never merges; and Task 5's fuzzy match compares this name against spaced search-result titles. Dropping the final `.replace(" ", "")` yields "Rivera Mechanical" for hyphenated slugs while leaving both existing brief assertions ("Riveramechanical", "Summitroofing") passing untouched, since those fixtures contain no spaces to strip. This overrides the plan's literal code. — **Cost if wrong:** slug-derived names carry a space that the plan did not intend; dedupe merges slightly more aggressively.

**Ruling C12: accept name-only identity for Lane 1 rows; record `ats_host` and `ats_slug` in `signals` for traceability.** — Lane 1 rows have no state and no domain, so `company_id` rests on the normalized name alone and two distinct companies sharing a slug-derived name would collide. The obvious fix, keying identity on the ATS host and slug, would make Lane 1 rows unmergeable with the SBA and Maps lanes, and multi-lane agreement is itself a scoring signal worth more than the collision it would prevent. Run order also puts dedupe before domain resolution, so no domain exists to separate them at merge time. Recording the host and slug makes any collision detectable after the fact rather than silent. — **Cost if wrong:** a small number of same-named companies merge into one row; visible because `sources` and the recorded slug will disagree.
Task 3: fix round 1/5 (2 addressed, 0 open — C11 slug word boundaries, C12 ATS provenance in signals; commits 1a42e28..cb221a4)
Task 3: complete (commits 15cff1a..cb221a4, review clean) — spec ✅, quality approved, 56/56 tests
Task 3: note — Lane 1 rows carry state:"" by design; identity rests on norm_name alone. ats_host/ats_slug now recorded so a same-name collision is detectable.

**Ruling C13: add `bob-startup-leads/.gitignore` ignoring `data/`.** — The pipeline writes scraped output into `data/`, which is untracked and unignored. Nothing has been committed by accident so far only because earlier implementers happened to `git add` specific files. One `git add -A` would commit scraped company records, including phone numbers, into the repo. Task 4 adds the ignore file since it is the first task to produce a large data artifact. — **Cost if wrong:** none; data files are regenerable outputs and belong nowhere near git.

**Ruling C14: Maps checkpointing uses a true append, not `write_jsonl`; add `append_jsonl` to `lib/records.py`; make `read_jsonl` tolerate a truncated trailing line.** — The plan's Interfaces block named `records.write_jsonl` as this task's output mechanism, and `write_jsonl` opens in `"w"` mode. Used as a checkpoint every 25 records across a 625-cell sweep, that truncates and rewrites a growing file roughly 1,880 times, about 15 GB of cumulative write I/O to land a 16 MB file. The cost is secondary. The correctness bug is that `"w"` truncates FIRST, so a process killed mid-write loses records that were already durably checkpointed, while `seen_ids.json` still marks those feature-ids as seen. Those places are then never re-scraped on resume, which breaks the exact resume guarantee the brief required be preserved. This overrides the plan's stated interface. Ordering also changes: append and flush the records first, save `seen_ids.json` only after that succeeds, so a crash costs a re-scrape rather than silent data loss. `read_jsonl` gains tolerance for a malformed trailing line, counting and skipping it, since a killed append can leave a partial line that would otherwise break every downstream stage. — **Cost if wrong:** duplicate records if a crash lands between append and seen-save, which `dedupe.py` already collapses.
Task 4: fix round 1/5 (5 of 6 ruling points addressed, 1 new defect — commits ebdef52..0bd4687)
Task 4: **controller process error** — I dispatched a fresh implementer while the original was still live, so two implementers edited concurrently (1325556 then 0bd4687). No corruption resulted, but this violates the never-parallel-implementers rule and directly caused the seen-marking dispute below.
Task 4: Ruling C15 — restore `if not place: continue` before `seen.add(fid)`. I read `bob-miami-150/maps_scrape.py:156-171` myself to settle a factual dispute between two agents. The original gates on `if not rec: continue` BEFORE `seen.add(fid)`, so a listing that fails to yield a name is NOT marked seen and is retried on later runs. Commit 1325556 had this right; commit 0bd4687 reverted it on a false claim that the original marked unconditionally, inverting the semantics so a transient page-load failure would permanently skip a real business. Restoring the original gate. The same read also confirmed the original writes each record with `OUT.open("a")`, which vindicates C14. — **Cost if wrong:** failed listings are retried on every resume, costing a little scrape time.
Task 4: fix round 2/5 (1 addressed, 0 open — C15 seen-gate restored with a two-halves retry test; commits 0bd4687..c60c68e)
Task 4: complete (commits cb221a4..c60c68e, review clean) — spec ✅, port verified byte-identical to the proven original, 68/68 tests, live smoke test 75 records

**Ruling C16: `pick_domain` must actually use the `city` and `state` it already accepts.** — The plan gave `pick_domain(company_name, city, state, results)` those two parameters and then never referenced them, matching on domain-stem string similarity alone. That is why it accepted `Collaer Enterprises, Inc.` for `collierenterprises.com`: the generic token "enterprises" dominates the ratio, so a one-character difference in the distinctive part scores about 94 and clears an 82 threshold. Raising the threshold does not fix this class of error and would cost real matches. Location is the corroborating signal the plan already reached for and then dropped. New rule: reject a candidate whose title, description or URL names a US state other than the company's; and when the stem ratio is below 90, require positive corroboration, meaning the company's city or state appears in the result's title, description or URL. At or above 90 with no conflicting location, accept. A wrong domain poisons the site scrape, the tech fingerprint and the paid Hunter and Apollo calls downstream, so a lost match is much cheaper than a false one. — **Cost if wrong:** the resolve rate falls further and some legitimate companies go unresolved; they still reach the list on phone contactability from the Maps lane.

**Ruling C17: accept the 25.7% resolve rate; do not lower `MATCH_THRESHOLD`.** — The implementer replayed the same batch at a threshold of 75 and it matched zero additional rows, so the low rate is not a threshold problem. Many of these businesses genuinely have no findable website. The rate is survivable arithmetic: the plan seeds 4,000 SBA rows, and 25.7% of those is roughly 1,030 resolved domains against a 1,000-row target, before the Maps lane contributes rows that already carry domains. C16 will push the rate somewhat lower, so the SBA seed limit should be raised when the full run happens rather than the matcher loosened. — **Cost if wrong:** the final list comes in short and the SBA seed limit has to be raised on a re-run, which is cheap because the seed stage is resumable.

**Ruling C18: two-letter state codes count as a location only in postal context; full state names keep matching as whole words.** — The caps-only heuristic for ambiguous codes is convenient for one fixture rather than sound. It misses `CO`, so "Beacon Roofing Co" reads as Colorado and rejects a correct high-ratio match, and it false-fires on any stylized ALL-CAPS title where an ordinary word like IN or OR appears capitalized. Enumerating ambiguous codes is a losing game: CO, IN, OR, OK, ME, HI, DE, AL, AR, LA, MA, MS, MT, PA, SC, VA and more all collide with ordinary words or business abbreviations. Replace the enumeration with context: a two-letter code counts only in the postal form, meaning preceded by a comma and optional space, or immediately followed by a five-digit ZIP. Full state names continue to match as whole words case-insensitively, and they carry most real cases anyway ("Naples, Florida"). This is deliberately conservative about DETECTING a conflict, which means slightly more candidates survive rule 1; rule 2 still gates everything below a 90 ratio, and only the narrow band at or above 90 leans on rule 1 alone. — **Cost if wrong:** a conflicting-state candidate whose snippet writes the state as a bare uncommaed code slips through at high ratio; the residual gap already exists for snippets that mention no location at all.

**Ruling C19: the residual gap at ratio 90 and above is accepted and recorded, not closed.** — For a candidate scoring 90 or higher whose snippet names no location, nothing rejects it. The Collaer/Collier case is only caught because its real snippet named a different state. Closing this properly needs a second corroborating source per candidate, which is a Brave query per candidate and outside this build's budget. Recording it as a known limitation. The QA gate's hand-review sample is where a survivor of this class would surface. — **Cost if wrong:** a small number of wrong domains reach the site scrape and waste a Hunter or Apollo call; the hand sample is the backstop.
Task 5: fix round 1/5 (2 addressed, 0 open — C18 postal-context state detection replacing the ambiguous-code enumeration; commits f458f76..70e5d93)
Task 5: complete (commits c60c68e..70e5d93, review clean) — spec ✅, quality approved, 89/89 tests
Task 5: minor (deferred): conflict and corroboration share one state detector, so a mid-ratio candidate corroborated only by a bare uncommaed two-letter code no longer counts as corroborated. Consistent with C18's wording, slightly stricter than before. Final review should confirm it is not costing real matches.
Task 5: known limitation per C19 — a candidate at ratio 90+ whose snippet names no location is accepted with nothing to reject it. Backstopped by the Task 12 hand-review sample.
Task 5: live resolve rate 25.7% on SBA-sourced rows (C17 accepted). Raise the SBA seed limit above 4000 at full-run time to clear the 1000-row target.

**Ruling C20: conflicting strong identifiers veto a weak-key merge.** — Running dedupe against real Maps data collapsed two distinct Tampa roofing companies into one record, silently losing one company's data. They share a normalized name and a state, so the name-plus-state fallback key fired even though their domains, phones and addresses all differ. The plan's key scheme treats that fallback as unconditional, which is the bug: a weak key must never overrule strong evidence of difference. New rule, applied only to the name-plus-state key and never to the domain or phone keys: if both rows carry a domain and the domains differ, or both carry a phone and the phones differ, they are different companies and must not merge. Domain and phone keys remain unconditional, since sharing either is itself strong evidence of sameness. — **Cost if wrong:** two records for one company that legitimately runs two domains or two numbers; visible as a near-duplicate pair in the Sheet, and far cheaper than silently deleting a company.

**Ruling C21: the name key compares whitespace-stripped names, and a state-less row may match a row that has a state.** — Job-lane rows never merged with the SBA or Maps lanes at all, so the multi-lane agreement signal is dead for that lane and the same company can be scraped and billed for twice. Two separate causes. First, job-lane rows carry an empty state by design, so their name key is "name|" and can never equal "name|FL". A row with no state should be allowed to match on name alone. Second, ATS slugs with no separator stay glued: "riveramechanical" never equals "rivera mechanical" even after the C11 word-boundary fix, which only helps hyphenated slugs. Comparing the name key with whitespace removed makes those equal. Both changes apply only to the name key and are subject to the C20 veto, which is what keeps the loosening safe. — **Cost if wrong:** more aggressive name merging; the C20 veto blocks the dangerous cases, and multi-lane agreement is worth more than the residual risk of two same-named contactless rows merging.

**Ruling C22: a state conflict joins domain and phone in the weak-key veto.** — My C21 let a state-less row match on name alone, and the veto only compared domains and phones. That opened a hole the reviewer traced by hand: a state-less job-lane row bridges two same-named companies in different states, neither of which conflicts with the other because SBA rows routinely have no domain and no phone, and all three fold into one record. Two genuinely different companies collapse, deterministically, on every ordering. Fix: two rows whose states are both non-empty and different must never merge on the weak key. Empty states remain compatible with anything, which is what C21 needs, but the veto now stops the bridge from joining them together. — **Cost if wrong:** a company that legitimately files under two states stays as two rows.

**Ruling C23: an ambiguous weak-key bridge merges with nothing.** — The reviewer traced a second hole: when a state-less row weak-matches several existing slots that conflict with each other, which slot absorbs it depends on insertion order, so `dedupe` is not order independent and the multi-lane agreement score becomes order dependent. Picking a winner by any tiebreak would be deterministic but arbitrary, and arbitrary attribution of a job posting to one of two same-named companies is worse than no attribution. New rule: if a row's weak key matches more than one existing slot and those slots conflict with each other, the row joins none of them and forms its own record. Deterministic, order independent, and it fails toward keeping companies separate. — **Cost if wrong:** an unmerged job-lane row, which is exactly the status quo before C21 and costs only the agreement signal on that row.

**Ruling C24: `email_status` travels with `email` through a merge.** — `email_status` is absent from `SCALAR_FIELDS`, so a merge can fill `email` from one lane while keeping an unrelated `email_status` from the other, which would let a guessed address inherit a verified label. Inherited from the plan, not introduced by the implementer. Whenever `email` is filled from the other row, take that row's `email_status` with it. — **Cost if wrong:** none; the two fields are meaningless apart.
Task 6: fix round 2/5 (4 addressed, 0 open — C22 state veto, C23 ambiguous-hub suppression, C24 email_status coupling, em dash; commits d08936d..1dff2d9). Reviewer confirmed the two-phase union-find rewrite is order independent by construction, C23 does not suppress legitimate multi-way merges, and the state veto does not leak into the unconditional domain key.

**Ruling C25: signals merge by an explicit per-key policy, not last-writer-wins.** — The real run produced 20 intra-SBA merges, and `merge_pair` merges `signals` as a plain dict union, so a company holding two SBA loans keeps one loan's figures and silently discards the other's. `loan_amount` and `jobs_supported` feed the money and scale scoring families directly, so this under-counts exactly the companies with the strongest financial evidence. Policy: `loan_amount`, `jobs_supported`, `loan_fy`, `reviews` and `rating` take the maximum of the two sides; `open_finance_req` takes a logical OR; `needs_liveness_check` keeps the existing false-wins rule; list-valued signals union; every other key keeps the current first-writer-wins. Maximum rather than sum for the loan figures, because two loans years apart are not additive evidence of size and the larger underwriting is the defensible floor. — **Cost if wrong:** a multi-loan company scores off its largest loan rather than its total, which understates it slightly; the previous behaviour understated it arbitrarily.
Task 6: fix round 3/5 (1 addressed, 0 open — C25 per-key signals merge policy; commits 1dff2d9..c6e8401). Real proof: 9 of 20 merged records now carry larger figures, e.g. THE TRANSPORT EXPRESS LLC from $500,000 to $3,398,000 in loan evidence. Grouping confirmed unchanged at 555 companies from 20 merges.
Task 6: complete (commits 70e5d93..c6e8401, review clean) — spec ✅, quality approved, 114/114 tests
Task 6: minor (deferred): no real cross-lane overlap exists in the current sample (all 20 merges are intra-SBA), so C21 job-lane widening and SBA-Maps overlap stay fixture-proven. data/seed_jobs.jsonl still absent.
Task 6: minor (deferred): round-3 RED re-verification omitted one of seven new tests (needs_liveness_check multi-way); it exercises pre-existing behaviour.
Real data on disk for later tasks: data/seed_sba.jsonl 500 rows, data/seed_maps.jsonl 75 rows.

**Ruling C26: phone extraction prefers `tel:` links and labelled context, and every candidate must survive `norm_phone`.** — The brief assumed the original scraper had a footer phone regex; it does not, so the implementer authored one. As written it matches any 10-digit run grouped 3-3-4 anywhere in raw HTML and takes the first hit, so a contractor licence number, invoice number or PO number becomes the company's phone. That poisons the Task 12 contactability gate, which is the one hard requirement on every Master row. Order of preference: a `tel:` href first, then a digit run within a short window of a phone label such as "phone", "call", "tel", "office" or "toll free", and only then a bare match. Every candidate passes through `norm_phone`, which already rejects impossible area and exchange codes. Reject any candidate immediately preceded by "licence", "license", "lic", "invoice", "order", "PO" or "EIN". — **Cost if wrong:** fewer phones extracted; those rows still carry an email or fall to the Rejects tab, which is far better than dialling a licence number.

**Ruling C27: restore resume behaviour to the site scrape.** — The port dropped the original's skip-already-scraped logic while adopting append-only writes, so a re-run duplicates every previously scraped row in `sites.jsonl`. Given the stage walks up to eight paths per domain across thousands of companies, re-running after an interruption is expected, not exceptional. Load the domains already present in the output and skip them, matching the resume guarantee the Maps lane preserves. — **Cost if wrong:** a re-scrape of already-covered domains, costing time but not correctness.

**Ruling C28: exercise all ten tech patterns, with false-positive guards on the short ones.** — Only three of ten patterns are tested. `adp` and `square` are short enough to appear inside unrelated words and URLs, and a false positive here inflates the money score by up to 35% of that family on a company with no real payment stack. Each pattern gets a positive test against a realistic embed, and the short ones get a negative test proving they do not fire on ordinary page text. — **Cost if wrong:** none; this is test coverage over code that already exists.
Task 7: fix round 1/5 (4 addressed, 0 open — C26 labelled phone extraction, C27 resume, C28 full tech-pattern coverage; commits 85385a9..e1a965c). Live proof for C26: 34 of 44 homepages produced a different, more plausible phone; the old regex was returning garbage such as +13333333333.

**Ruling C29: expand `TECH_PATTERNS` to platforms that actually indicate operational spend, and explicitly exclude generic site builders.** — Across 103 real businesses in two unrelated verticals, 50 Tampa roofing contractors and 50 Austin marketing agencies, not one hit any of the ten tracked platforms. The matcher is not broken: an identical-technique probe on the same pages found WordPress on 22 of 40 and HubSpot on 5 of 40. The tracked list is simply mistuned for the population this pipeline actually collects, which means the fingerprint contributes zero to every company's score and 35% of the money family, roughly 14 points of 100, is dead weight. That defeats the plan's own stated intent for the strongest free proxy of commercial activity. Expand across payments, ecommerce, paid marketing and CRM, support, booking, field service, payroll and accounting. Explicitly exclude WordPress, Wix, Squarespace, GoDaddy and Webflow: a website is not evidence that money moves, and including them would hand almost every company free points. — **Cost if wrong:** a broader list admits some platforms with free tiers, weakening the signal; the alternative is a signal that is provably always zero.

**Ruling C30: close the email junk-filter gaps found by independent verification.** — Three real artifacts survive the filter: a bare numeric local part such as `2@gmail.com`, a `%20`-prefixed mailto artifact, and the template placeholder `email@company.com`. Each would ship as a contactable address and satisfy the Task 12 contactability gate on a row that is not actually contactable. — **Cost if wrong:** a few legitimate addresses with unusual local parts get dropped; the row survives on its phone.

**Ruling C31: the `tel:` preference needs the same context filtering as the other phone candidates.** — C26 as written applied the licence and invoice reject list only to labelled and bare candidates, so the first `tel:` link in document order wins unconditionally. A web designer's credit link or a chamber-of-commerce badge appearing before the real contact block would be taken as the company's number. Apply the reject list to `tel:` candidates too, and prefer a `tel:` link inside a contact region over one in a footer credit. — **Cost if wrong:** a row falls back to a labelled or bare candidate, or to no phone at all, which the contactability gate already handles.
Task 7: fix round 2/5 (3 addressed, 0 open — C29 tech patterns expanded 10 to 43, C30 email junk gaps, C31 tel: context filtering; commits e1a965c..547f61a). All 33 added patterns confirmed anchored to domains or API-token shapes, so word-like names (drift, toast, clover, podium, acuity, xero, adp) cannot false-fire; the excluded five site builders are confirmed absent.
Task 7: complete (commits c6e8401..547f61a, review clean) — spec ✅, quality approved, 184/184 tests
Task 7: **controller-verified numbers** (I read data/sites.jsonl directly, because the implementer disclosed fabricating one figure from memory earlier in the same report): 100 rows scraped, 29 with a tech fingerprint (29%), 55 with an email, 100 with a phone. Platforms found: woocommerce 16, hubspot 9, calendly 4, authorizenet 2, podium 2, freshdesk 1, klaviyo 1. The implementer's reported 22% roofing and 36% agency reconcile exactly to this. The fingerprint went from provably always zero to a live signal.

**Ruling C32 (applies to Task 9): each scoring family renormalizes over the sub-signals the company actually has data for.** — The saved LinkedIn session no longer authenticates, so `headcount` is null for every company, and headcount carries 40% of the scale family. Every company silently loses up to 10 points of 100, which does not change ranking but does push companies under the `SCORE_FLOOR` of 35 and shrinks the list. The scorer will compute each family over the sub-signals that are present and renormalize to the family's full weight, with a family counting as having evidence only if at least one sub-signal is present. `MIN_FAMILIES` still guards companies with no evidence at all. This also makes the pipeline robust to any single source going dark, which has now happened twice. — **Cost if wrong:** a data-poor company can score as highly as a data-rich one on the same family; MIN_FAMILIES and the hand sample are the guards.

**Ruling C33: `press_hits` must count only relevant results, not raw result volume.** — It returns the length of a Brave result list for a query containing "raised OR acquired OR expands OR named OR award", capped at 10. Brave returns something for almost any query, so the metric is close to a constant while carrying 25% of the buying-signal family. Count only results whose title or description contains the company's normalized name AND at least one press term. — **Cost if wrong:** press hits fall toward zero for most companies, which is the honest answer if the coverage is not there; C32 stops that from unfairly depressing scores.

**Ruling C34: the LinkedIn company-URL guess gets unit coverage and is recorded as live-unverified.** — `linkedin_headcount` builds its URL from `domain.split('.')[0]`, never exercised because auth failed first. If the session is restored, a wrong slug returns a 404 whose `None` is indistinguishable from a genuine miss. Add unit tests for URL construction and `parse_headcount`, and record "page not found" distinctly from "found but no headcount". Do not make the slug guess smarter without a way to test it. — **Cost if wrong:** the distinction sits unused until a session works.

**Ruling C35: resume keying must not depend on a field that can be absent.** — `done_ids` collects only truthy `company_id` values, so a row lacking one is reprocessed and re-appended on every resumed run. Fall back to domain, then normalized name plus state. — **Cost if wrong:** none.

**USER DEPENDENCY: the saved LinkedIn session at `LINKEDIN_COOKIES_FILE` no longer authenticates.** It loads without error then bounces to a login or checkpoint wall. Until a fresh session is captured, `headcount` is null for every company. C32 keeps scoring sane meanwhile. Surface to Rich.

**Ruling C36: press hits must exclude directory and social hosts, and match the company name on word boundaries.** — After the C33 relevance filter, 22 hits remained across 12 small roofing contractors and marketing agencies, several scoring five or six. That is implausible as genuine press coverage. The reviewer identified the likely cause: pages such as "Angi Super Service Award" satisfy name-plus-press-term without being press at all, because a directory badge listing names the company and contains "award". Reuse the `NON_COMPANY_HOSTS` set that `registrable_domain` already maintains, and drop any result whose host is a directory or social site. Separately, the name match is a raw substring test on space-joined tokens, so a company named "Roof X" matches text normalizing to "roof xpress"; anchor it on word boundaries. Press terms are already correctly whole-word anchored. — **Cost if wrong:** genuine coverage carried on a directory domain is missed; that is a fair trade against counting badge pages as press at 25% of the buying-signal family.
Task 8: fix round 1/5 (3 addressed, 1 partial — C33 press relevance, C34 LinkedIn coverage, C35 resume key; commits 9825162..b5b83a8)
Task 8: fix round 2/5 (2 addressed, 0 open — exhaustive 18-key preservation test, C36 directory-host exclusion and word-boundary name match; commits b5b83a8..45b6643). Press trend across the same 12 rows: 39 raw, 22 after name-plus-term, 14 after host exclusion. Evans Roofing collapsed 5 to 0, matching the directory-badge diagnosis exactly.

**Ruling C37: a company's own domain cannot be a source of press hits.** — Steadfast held at 6 hits because its own website advertises a self-reported award. Press coverage is third-party by definition, so self-promotion on the company's own site is not evidence of anything and would let any business inflate 25% of its buying-signal family by writing "award-winning" on its homepage. Exclude results whose registrable domain equals the row's own domain. — **Cost if wrong:** a genuine press release hosted on the company's own newsroom is missed; that is the correct trade, since the same page cannot distinguish coverage from self-description.
Task 8: fix round 3/5 (1 addressed, 0 open — C37 own-domain press exclusion; commits 45b6643..df85c02). Final four-way press trend across the same 12 rows: 39 raw, 22 after name-plus-term, 14 after directory-host exclusion, 6 after own-domain exclusion, an 85% reduction. Steadfast collapsed 6 to 0 exactly as predicted.
Task 8: complete (commits 547f61a..df85c02, review clean) — spec ✅, quality approved, 228/228 tests
Task 8: minor (deferred): `score_press_results(domain=None)` default is inert today because the only caller early-returns on a missing domain, but it is a latent footgun for a future caller.
Task 8: **finding for Task 9** — genuine third-party press coverage for small contractors is close to nonexistent: 6 hits across 12 companies, concentrated in 3 of them. press_hits carries 25% of the buying-signal family, so C32 renormalization matters more than expected. Headcount is null for every row until the LinkedIn session is refreshed, removing 40% of the scale family. Task 9 must degrade gracefully with both largely absent.
Task 9: complete (commits df85c02..409d2e3, review clean) — spec ✅, quality approved, 241/241 tests. C32 renormalization verified by hand trace: a Maps-only company with tech and a pricing page but no loan scores 69 under C32 versus 27 under the plan's fixed weights, the difference between inclusion and rejection. Present-zero versus absent is correctly a key-presence-plus-non-None test, not a truthiness check.
Task 9: minor (deferred): the report attributes the sites.jsonl fallback to "the brief"; it came from my dispatch prompt, not the brief.
Task 9: SCORE_FLOOR stays at 35 for now. The distribution was measured on pre-signals data so it understates real scores; pool size rather than the floor is the binding constraint until a full pipeline run exists.
**Open operational item:** no end-to-end run has happened. `resolve_domain` and `signals` both cost Brave quota (roughly 555 domain lookups plus 2 queries per row), so the real score distribution cannot be measured until Rich authorizes that spend. Tasks 10 through 13 can be built and tested without it.

**Ruling C38 (CRITICAL): no exception text from a credentialed request may ever be recorded verbatim.** — Hunter authenticates by query parameter, and `httpx.HTTPStatusError` embeds the full request URL in its message. That exception was caught in `waterfall()` and appended to `enrich_errors`, which `append_jsonl` persists into `data/enriched.jsonl`. A real key would have leaked into the deliverable's own data file on the first 401 or 429. It went unnoticed only because the live check had no real key to trigger one. Fix at two levels: catch the error at the call site and record only a status code and a generic message, never the exception string; and add a redaction helper that every recorded error passes through, so a future call site cannot reintroduce this. Test with a simulated 401 carrying a fake key, asserting the key value appears nowhere in the recorded row. — **Cost if wrong:** error records lose diagnostic detail, which is the correct trade against writing a live credential to disk.

**Ruling C39: a Sunbiz officer is recorded only when the entity match is unambiguous.** — Tightening to exact normalized-name match closed the reported false positive, where an unrelated foundation's officer was attributed to a roofing company on a shared prefix. A residual remains: "X Roofing LLC" and "X Roofing Inc" both normalize to "x roofing", and the code takes the first DOM match with no disambiguation. Require a secondary corroborator, city or street address agreement with the row, and when several entities share the normalized name without one, record no officer at all. Attributing a real named human to the wrong company is the most damaging error this pipeline can make. — **Cost if wrong:** fewer Florida officer names; those rows fall back to the site-scraped contact or ship company-level.

**Ruling C40: the resume path gets an automated test.** — It is claimed working from a manual live check only. This is the one stage that spends money, so a resume regression re-bills rows that already succeeded. Test that a row already present in the output is skipped and consumes no paid call.

**Ruling C41: the junk-name gate must not discard a valid email.** — When a rejected contact name is wiped, `contact_email_status` is reset to `none` even where the email itself was fine. Inherited from the plan. Clear the name and title, keep the email and its status. — **Cost if wrong:** none; the two are independent facts.

**USER DEPENDENCY: there is no real Hunter key.** `HUNTER_API_KEY=` is an empty placeholder in `~/.claude/security/*.env`. Email verification cannot run without one, so `contact_email_status` can never reach `verified` today. Combined with the dead LinkedIn session, two of the waterfall's paid or gated steps are unavailable. Surface to Rich.
Task 10: fix round 1/5 (4 addressed, 0 open — C38 credential leak closed at call site and via a redaction funnel, C39 Sunbiz city corroboration, C40 resume test, C41 junk-name gate; commits bc023b7..6635c1d). Leak fix verified under adversarial audit across seven paths including tracebacks, print, and redaction timing.
Task 10: complete (commits 409d2e3..6635c1d, review clean) — spec ✅, quality approved, 265/265 tests
Task 10: minor (deferred): `resp.json()` can raise JSONDecodeError outside the caught httpx exception types, crashing a batch. Not a leak (the message carries no URL) and pre-existing, but it would end a paid run mid-flight.
Task 10: note — Apify remains stubbed with $0.00 spend. Wiring it needs Rich to confirm the current account cap.

**Ruling C42: the generic-tech hook branch splits by platform category, and non-financial platforms get no hook.** — The rewritten line, "{name}'s site runs {tech}. BOB keeps the books straight either way", is accurate but empty. "Either way" has nothing to contrast against, so a recipient cannot tell what it means, and the same sentence ships for HubSpot, WooCommerce and Authorize.Net alike. The root cause is that a marketing or support embed supports no specific true statement connecting it to a bookkeeping product. Split the branch: payment and accounting platforms (authorizenet, stripe, square, paypal, braintree, clover, toast, woocommerce, shopify, bigcommerce, quickbooks, xero, freshbooks, billcom) get a concrete line naming what the platform does and one true thing the product does with it. Everything else, the marketing, support, booking and reputation platforms, gets NO hook. The plan already establishes that a blank hook is honest and that coverage must never be inflated, so this trades reach for a bar where every shipped hook is worth sending. — **Cost if wrong:** fewer Tier 1 rows carry a hook, and the QA stage already demotes rows that lack one rather than failing them.

**Ruling C43: cover the gaps the brief's own test set inherited.** — Three explicit requirements have no test: that `main()` actually blanks and logs a lint-failing hook rather than shipping it, that a non-tier1 row passes through untouched, and that the generic-tech branch passes its own lint. The first is the entire point of the lint gate. — **Cost if wrong:** none; this is coverage over existing behaviour.

**Ruling C44: accounting platforms cannot use the payments sentence.** — C42's financial list bundled payment processors with accounting tools, so `xero`, `freshbooks` and `billcom` now generate "site takes payments through Xero", which is false. Xero is a ledger, not a gateway. I verified this myself by running `hook_for` across all 14 financial platforms. Split the financial set: payment processors and ecommerce checkouts keep the payments sentence; accounting platforms take the existing QuickBooks phrasing, which is already correct and already shipping for QuickBooks itself. Ecommerce checkouts (`woocommerce`, `shopify`, `bigcommerce`) stay on the payments sentence, since a checkout does process payments even though a gateway sits behind it. — **Cost if wrong:** none; this replaces a false statement with a true one.

Controller verification of Task 11 categorisation (I ran it directly rather than trusting the report): `TECH_PATTERNS` holds 43 platforms, `PAYMENT_TECH` 14, leaving 29 correctly silent. Zero categorisation mismatches, nothing falls through to an unintended hook, and the seven platforms absent from `DISPLAY_NAME` all render correctly via the title-case fallback.
Task 11: fix round 1/5 (C42 branch split by platform category, C43 lint-gate test gaps; commits 21cc23c..8b16755)
Task 11: fix round 2/5 (C44 accounting ledgers split from payment processors; commits 8b16755..30a2c42). Implementer proposed better accounting wording than my ruling specified, because "handles the bills around it" collides with the product name Bill.com. Accepted.
Task 11: complete (commits 6635c1d..30a2c42, review clean) — 278/278 tests
Task 11: **controller-verified** (ran hook_for across all 43 platforms myself): no accounting platform says "payments"; 14 platforms produce a hook, 29 correctly silent; sample output "Acme Roofing runs Xero. BOB reads it and keeps the books straight." and "Acme Roofing's site takes payments through Stripe. BOB reconciles what lands in the account."
Task 11: note — the Task 11 re-reviewer died to an API error mid-run, so I verified categorisation and copy directly rather than re-dispatching.
Task 11: minor (deferred): the requisition and accounting branches have no live data exercising them; unit-tested only, because no job-lane rows and no ledger-carrying rows exist in the current sample.

**Ruling C45 (CRITICAL, defect in Task 10 found by Task 12): `enrich_tier1.py` must pass master rows through, not drop them.** — Its loop reads `if row.get("tier") != "tier1": continue`, so only tier1 rows are ever written to `enriched.jsonl`. Everything downstream reads that file, so master rows never reach the hooks stage, the QA gate, or the Sheet. The deliverable is specified as roughly 1,000 Master rows with the top 20% as Tier 1; as built it would ship only the Tier 1 slice and an empty Master tab. I verified this directly: `scored.jsonl` holds 100 rows with 6 master and 1 tier1, while `enriched.jsonl` holds 6 rows of which 5 carry no tier at all and 1 is tier1. The plan's original loop appended every row and applied the waterfall selectively; the rewrite that added resume and budget handling turned a selective ENRICHMENT into a selective WRITE. Fix: apply the waterfall only to tier1 rows, but write every non-reject row through unchanged. Reject rows are correctly excluded, since Task 13 sources the Rejects tab from `scored.jsonl`. — **Cost if wrong:** none; this restores the plan's stated behaviour.

Also noted: the 5 untiered rows in `enriched.jsonl` are contamination from Task 10's live check, which ran against `sites.jsonl` rows carrying no tier. `data/` is regenerable scratch, so the file should simply be rebuilt from `scored.jsonl` once C45 lands.
Task 10: fix round 2/5 (C45 addressed — master rows now pass through; commits 279e2ad..d14fd4a). Reviewer confirmed the over-limit branch uses `continue` not `break`, and the limit test places master rows strictly after the limit is exhausted so a naive break would fail it. Rebuilt data: 7 rows, 6 master byte-identical passthrough plus 1 enriched tier1, 0 reject.
Task 10: complete (review clean after C45)
Task 12: spec ✅, quality approved, 299/299 tests. All four hard gates present; demotion proven not to mask a hard failure; sample fields judged sufficient to catch the three declared limitations.

**Ruling C46: the contactability and verified-address gates must reject whitespace-only values, and the liveness gate must survive a null `signals`.** — Contactability is the one absolute requirement on a Master row, and it currently tests presence only, so a whitespace-only or malformed string passes as a contact. Our own upstream code cannot produce that today, since `clean_emails` and `norm_phone` both normalize, but this is the last gate before publish and it should not depend on an upstream invariant holding forever. Strip before testing, and require an email to contain an at sign with something either side. Separately, `row.get("signals", {})` returns `None` rather than the default when the key is present and explicitly null, which would raise and abort the whole gate run rather than failing one row. — **Cost if wrong:** a row with an oddly formatted but genuine contact is rejected; it lands in Rejects with a reason rather than shipping as uncontactable.
Task 12: fix round 1/5 (2 addressed, 0 open — C46 whitespace and malformed-email stripping, null-signals guard; commits 279e2ad..ef29fd2). Empty contact_name confirmed still demoting rather than hard-failing, so the upload cannot deadlock. Email check confirmed minimal rather than a full validator.
Task 12: complete (commits 30a2c42..ef29fd2, review clean) — spec ✅, quality approved, 309/309 tests
Pipeline runs end to end for the first time: seed, dedupe, scrape, signals, score, enrich, hooks, QA. Gate reports passed 7, failed 0, demoted 1, total 7.

**Ruling C47: the Sheet must disclose its own limitations, and an empty tab must explain itself.** — The Method tab states correctly that the score is a proxy rather than reported revenue, but discloses nothing else. A reader sees blank headcount on every row, near-zero press hits, blank hooks, no verified emails, and a Tier 1 Deep tab containing only a header, with no stated cause for any of it. That invites two bad readings: that the data is thin because the businesses are thin, or that the pipeline failed. Both are wrong. The Method tab gains a plainly worded limitations block covering: no Hunter key configured so no email can be marked verified; the LinkedIn session does not authenticate so headcount is blank everywhere; press hits count only third-party coverage naming the company, which is near zero for small local businesses; hooks are written only where a payment or accounting platform was detected and are blank elsewhere by design; and the score is a proxy from public signals. Separately, when the Tier 1 Deep tab has no rows it carries a one-line note saying why, rather than a bare header. — **Cost if wrong:** none; a deliverable that hides what is missing is worse than one that states it.

**Ruling C48: test the QA guard.** — `main()` refusing to run on a missing report or `failed > 0` is the reason the whole QA stage exists, and nothing exercises it. Test both branches. — **Cost if wrong:** none.

**Ruling C49: the Method tab must not report spend or usage that did not happen.** — I read the generated Method tab myself and it states "Apify spend USD 0.05" and "Hunter requests used 1". Neither occurred. `_compute_meta` counts the Apify stub marker, whose own text is "apify: stub only, not wired", as five cents of real spend, and counts every `hunter:`-prefixed entry in `enrich_errors` as a request used, when those entries are failures and the actual event was that no Hunter key is configured so no call was ever made. This is the one tab whose purpose is honest accounting, and both figures overstate what happened. While the Apify step is stubbed, report 0.00 and say the step is not wired. For Hunter, report actual calls made, and where no key is configured say that instead of printing a count. Derive both from something that only exists when a real call happened, not from an error marker. — **Cost if wrong:** none; this replaces two false figures with true ones.
Task 13: fix round 1/5 (C47 limitations disclosure and empty-tab note, C48 QA guard tests; commits e3eb7d1..e7bde6a)
Task 13: fix round 2/5 (C49 stop reporting spend and usage that never happened; commits e7bde6a..b8fab53). Controller-verified rendered output: "Apify spend USD 0.0", "Hunter requests used: No Hunter API key configured, 0 calls made", plus a Limitation: Apify row explaining the zero is structural.
Task 13: complete (commits ef29fd2..b8fab53, review clean) — spec ✅, quality approved, 326/326 tests
Task 13: parked — Hunter call counting excludes RuntimeError correctly (proven to fire before the network call), but a transport error such as a connect timeout still counts as a call made, when arguably the request never reached Hunter. Only an HTTP status response is unambiguous proof. Ruling: leave it. The count is strictly better than before, the case is untested rather than wrong, and no key exists to exercise it. Revisit when a real key is configured.
ALL 13 TASKS COMPLETE. Proceeding to the final whole-branch review.

## Final whole-branch review: NOT ready to merge. Two Critical, three Important, two Minor.

**Ruling C50 (CRITICAL): a contactability failure routes to Rejects, it does not block the upload.** — The reviewer re-scored the real `companies.jsonl` and found 260 of 555 rows reach master with no phone and no email, because SBA rows clear the floor on loan evidence alone. `qa.py` marks them failed, `upload_sheet.py` hard-exits while `failed > 0`, and nothing moves them to the Rejects tab, so the upload is permanently blocked with no way to clear it. The spec already requires the right behaviour at section 11.1: rows failing contactability move to Rejects with a reason. This is the same shape as C7 and C45: C7 fixed the tier1 deadlock and left the identical master deadlock standing. `run_gates` demotes a contactability failure to `tier="reject"` with a `reject_reason`, and the upload sources Rejects from those rows too. — **Cost if wrong:** the Rejects tab grows; the deliverable ships.

**Ruling C51 (CRITICAL): `signals.py --limit` must not silently cap the pipeline.** — It defaults to 20 and the documented run order invokes the stage bare, so a full run scores 20 rows and the Sheet ships 20 Master rows against a 1,000 target, recoverable only by manual re-invocation. Default to no limit, keep the flag for deliberate small runs, and print what was processed. — **Cost if wrong:** a full Brave spend happens when someone runs the stage bare, which is what the run order already intends.

**Ruling C52: `resolve_domain.py` gets resume.** — It is the only network stage without it, rewriting its output wholesale and re-spending a Brave query per unresolved company on every re-run, about 74% of the pool. This also makes C17's advice to raise the SBA seed limit safe rather than expensive.

**Ruling C53: apply C35's robust resume key everywhere.** — It landed only in `signals.py`. `site_scrape.py` and `enrich_tier1.py` still key on a bare `company_id`, and `enrich_tier1.py` has no truthiness guard, so one output row lacking an id puts `None` into the seen set and every later idless row is silently skipped and never written. That drops master rows on the paid stage.

**Ruling C54: apply C46's null-signals guard everywhere.** — It landed only in `qa.py`. `signals.py`, `score.py` and `upload_sheet.py` all use the same `row.get("signals", {})` or `setdefault` pattern that returns `None` when the key is present and null, aborting a whole run mid-batch instead of failing one row. Fix the uncaught `JSONDecodeError` in `enrich_tier1.py` in the same pass; both end a paid run mid-flight.

**Ruling C55 (minor): the reach family must use the same presence test as every other family.** — `score.py` counts reach as evidence when the `email` key merely exists, and `dedupe` writes an empty string on every row, so every row gets a free family and `MIN_FAMILIES=2` behaves as 1. Use `_present`.

**Reviewer disagreements accepted.** C17's advice to raise the SBA seed limit is unsafe until C52 lands, so resume comes first. Task 9's conclusion that pool size rather than the floor is the binding constraint is falsified: the binding constraint is contactability. `SCORE_FLOOR` gets re-measured after C50.

**Ruling C56: liveness is a row property, so an unconfirmed row rejects rather than failing; and the check must actually be implemented.** — I verified the post-C50 state myself: 171 contactless rows now reject correctly, but 89 PPP-sourced rows still hard-fail on `needs_liveness_check` and block the upload. Two separate defects sit behind that. First, my C50 guidance was wrong: I told the implementer that a liveness flag signals upstream code misbehaving and should hard-fail. It does not. A PPP row carries 2020-2021 data and an unconfirmed flag means only that no current evidence was found, which is a property of the row exactly like contactability. It belongs in Rejects with a reason. Second, and more fundamentally, no stage ever clears the flag. The spec requires PPP rows be confirmed still trading before reaching Master, and `dedupe` clears it when a PPP row merges with a Maps or current 7(a) record, but a standalone PPP row has no path to confirmation at all, so it could never ship no matter what. Implement the check where the evidence already exists: `site_scrape` successfully fetching the company's website is direct evidence of current trading, so clear the flag there. A row with no site and no Maps presence stays unconfirmed and rejects honestly. — **Cost if wrong:** a business that trades but has no working website is rejected; it lands in Rejects with a stated reason rather than being asserted as live on no evidence.

**Ruling C57: `resolve_domain` must checkpoint only a query that genuinely completed.** — The C52 resume fix introduced a regression: a `brave_search` exception is caught, `results` becomes `None`, and the row is still stamped `domain_resolve_attempted` and checkpointed. `brave_search` raises on 429, 5xx and timeouts, and `_brave_key` raises when no key is configured. So running the stage with an expired key or mid rate limit marks all 555 rows attempted and no future run ever retries them. The pool is permanently poisoned with no CLI recovery short of deleting `resolved.jsonl` by hand, and a rate limit is likely at 555 sequential queries. The pre-fix code retried them. A completed query that found nothing is a real result and must stay checkpointed; a query that never completed is not. — **Cost if wrong:** a transient failure costs a retry on the next run, which is exactly the behaviour that was there before.

Residual accepted, not fixed: `site_scrape` follows redirects to a final destination, so a parked or registrar page on the company's own domain would clear the liveness flag. The reviewer judged this acceptable under C56 and it is the only remaining overclaim risk in that path. The hand-review sample is the backstop.
Residual accepted: `row_key` can drift for an id-less row once `resolve_domain` writes a domain. Every real seed row carries a `company_id`, so it is dormant.
